# 设计说明

这个项目的前端形态不是普通网页，而是“网页技术实现的桌面桌宠式入口”。React 负责界面，Electron 负责把界面放到桌面上的小窗口中。

## 核心交互

- 闲置态：桌面右下角显示一个小型桌宠入口，只展示待处理数量。
- 展开态：点击桌宠后，从右侧展开 360px 待办面板。
- 自动收起：窗口失焦后回到桌宠入口。
- 手动收起：面板标题栏保留收起按钮。
- 顶部主导航：展开后先选择 `今日待办` 或 `周期计划`。
- 今日待办：显示手动待办，以及今天日期命中的周期计划条目。
- 周期计划：展示计划卡、日期条目和内容块详情，但第一版不展示具体时间。

## 视觉策略

- 桌宠入口负责轻量存在感，不持续打扰用户。
- 待办面板负责效率，布局保持紧凑、清晰、可扫描。
- 卡片只用于待办条目、摘要块和菜单，不把整页包进大卡片。
- 动效只表达状态变化：展开、收起、添加、完成和同步状态变化。

## 信息架构

`今日待办` 第一屏优先回答三个问题：

1. 当前还有多少待处理事项。
2. 已经完成多少事项。
3. 总共有多少事项。

今日列表来源规则：

```text
今日待办 = 手动待办 + date 等于今天的周期计划条目
```

内部筛选仍然只区分 `待处理 / 已完成 / 全部`。

`周期计划` 的层级是：

```text
计划卡 -> 日期条目 -> contentBlocks 详情
```

## 当前阶段边界

本阶段重点是前端体验、本地可用性和统一数据结构。周期计划使用本地 mock 和 `localStorage`，真实 Hermes、飞书、AI 安排和 Windows 通知暂不联网执行，只保留可替换的模块接口。
