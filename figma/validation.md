# Figma 验收记录

## 2026-07-10 当前主流程清理版

- 已清理 `01 UI States` 旧画板，只保留当前 3 个主状态。
- 已清理 `02 Components` 旧组件素材，只保留当前版本组件集合。
- 已清理 `03 Motion Spec` 旧说明，只保留当前抽屉动效说明。
- 已清理 `04 Handoff Notes` 旧交接内容，只保留当前实现规则。

## 当前节点

- `最新版本 / 今日待办 - 从周期任务读取`：节点 `48:2`。
- `最新版本 / 周期任务 - 主题列表`：节点 `48:55`。
- `最新版本 / 周期任务 - 健身抽屉打开`：节点 `48:90`。
- `最新版本 / 当前组件`：节点 `48:157`。
- `最新版本 / 动效说明`：节点 `48:205`。
- `最新版本 / 交接说明`：节点 `48:212`。

## 视觉验收

- 顶部导航只有 `今日待办 / 周期任务` 两个入口。
- `今日待办` 画板明确写出：今日待办 = 手动待办 + 周期任务今日命中。
- `周期任务` 画板只展示主题卡，不把健身明细直接展开在主题卡下方。
- `健身抽屉打开` 画板展示左侧主题列表和右侧详情抽屉。
- 右侧抽屉中包含 7 天条目和 `fitness.exercise_list` 内容块。
- 画板位置按横向顺序排列：今日待办、周期任务、健身抽屉打开，没有互相重叠。
- 当前主流程不再包含右下角桌宠入口。

## 数据交接验收

- 今日待办读取规则：`CycleTaskEntry.date === today`。
- 第一版不展示 `time` 字段。
- 主题模型：`CycleTaskTopic -> CycleTaskEntry -> contentBlocks`。
- 健身专用块：`fitness.exercise_list`。
- 未知块：通用 JSON/Markdown 预览。
