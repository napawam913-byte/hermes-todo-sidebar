# Figma 验收记录

## 2026-07-20 Neutral Glass 中性玻璃定稿

- 已建立 `Neutral Glass / Tokens` 变量集合，共 `16` 个固定中性视觉 Token。
- 已建立 `SegmentedControl`、`IconButton`、`PrimaryButton`、`TodoCard`、`CyclePlanCard`、`DetailPane` 和 `CharacterPicker` 可编辑组件；`SegmentedControl` 已包含今日与周期两个变体。
- `01 UI States` 当前四个主画板为 `179:101`、`179:131`、`179:160`、`179:201`，横向排列且互不重叠。
- 宽屏与窄屏统一使用 `#007AFF`、中性冷白面板和相同卡片材质，不再读取企鹅橙色或其他角色主题色。
- 默认面板不透明度为 `86%`，允许范围固定为 `72%–94%`。
- 宽屏详情采用约 `40% / 60%` 主从布局；`360px` 状态使用同面板单页结构。
- 已分别在白色文档、蓝灰照片和深色壁纸背景上截图检查；文字、按钮和卡片均保持可读。
- 已修复所有 `1px` 文字图层；复查未发现异常文字高度、画板越界或缺失字体。
- 四个成品面板都同时应用 `DROP_SHADOW` 与 `BACKGROUND_BLUR 28px`。
- 四个主画板分别包含 `4 / 4 / 5 / 4` 个公共组件实例，不再复制第二套卡片和导航样式。
- Token 规范板节点为 `176:2`；公共组件验收板为 `177:43`，高度 `800px`、不裁切，完整显示 7 个组件实例。
- 外观设置已使用项目内真实企鹅缩略图验证角色裁切和选中状态。
- 已按用户确认新增 `Color/Text/OnGlassSecondary = #3A3A3C`，仅用于直接位于透明面板上的小字；卡片内仍保留 `#6E6E73`。
- 以深色背景 `#171A22`、面板透明度 `72%` 计算，玻璃表面次级文字对比度为 `5.79:1`；蓝灰和白色背景分别为 `7.51:1` 与 `10.8:1`。
- 最终自动 QA 显示：四画板异常文字高度、缺失字体、文字越界和旧玻璃表面次级文字残留均为 `0`。
- 四张画板、Token 板和组件验收板已重新截图，内部审查问题全部修复，可进入用户视觉验收。
- 独立只读复审结果：`P0 / P1 / P2` 均为零；组件验收板保留的通用灰色角色占位属于 `P3`，正式外观画板已用真实企鹅资源验证，不阻塞交接。

## 2026-07-13 可拖动桌宠与锚定面板版

- 已清理 `01 UI States` 旧画板，只保留当前 4 个桌宠状态。
- 已清理 `02 Components` 旧组件素材，只保留 3 个可编辑 Component Set。
- 已清理 `03 Motion Spec` 旧说明，只保留拖动、展开和收起动效规范。
- 已清理 `04 Handoff Notes` 旧交接内容，只保留锚定面板实现规则。
- 已同步更新 `00 Cover`，移除旧“右侧常驻侧边栏”描述。

## 当前节点

- `最新版本 / 桌宠自由拖动`：节点 `66:2`。
- `最新版本 / 向下展开今日待办`：节点 `67:26`。
- `最新版本 / 向上展开周期计划`：节点 `70:49`。
- `最新版本 / 同面板计划详情`：节点 `71:72`。
- `最新版本 / 锚定桌宠组件`：节点 `62:2`。
- `最新版本 / 可拖动桌宠动效规范`：节点 `72:2`。
- `最新版本 / AI Flow 交接说明`：节点 `95:5`。

## 视觉验收

- 四个桌宠状态画板均为 `960px x 860px`，按 `100px` 间距横向排列，没有重叠。
- 向下展开和向上展开均完整显示锚定面板，没有底部裁切。
- 桌宠与面板保持 `8px` 锚点间距，桌宠本体在展开后继续可见。
- 周期计划详情在同一面板内切页，不再扩展为固定双栏。
- `DesktopPet`、`AnchoredTodoPopover`、`DetailPageHeader` 均为 Component Set。
- 所有本轮新增 Figma 文本均使用 `Noto Sans SC`，未发现文本溢出。

## 数据交接验收

- 拖动阈值：X 或 Y 任一方向达到 `4px` 才进入拖动。
- 布局快照：`direction`、`panelWidth`、`panelHeight`、`petOffsetX`、`petOffsetY`。
- 位置文件：`pet-position.v1.json`，独立于待办和周期计划数据。
- 双屏规则：面板与桌宠位于同一显示器，显示器消失时回退到主屏。
- 测试副本数据目录：`%APPDATA%\hermes-todo-sidebar-test`。

## 2026-07-15 AI Flow 定稿

- `01 AI Flow` 页面节点 `93:6`，只保留同一套大模型待办流程。
- API 未配置、模型追问、提案预览、成功/回滚和 `360px` 紧凑状态分别位于节点 `94:8`、`94:11`、`94:14`、`94:17`、`94:20`。
- 宽屏主参考为约 `854px x 581px`，对话与提案比例约为 `36% / 64%`；紧凑模式不横向溢出。
- 提案预览明确区分新增、修改、完成、恢复、跳过和永久删除，删除在清单与确认区均使用危险样式。
- 成功状态同时提供 `查看今日待办` 和 `查看周期任务`，新增按钮节点为 `139:166`。
- 失败状态明确显示整批回滚，并对应 `target_missing`、`version_conflict`、`validation_failed`、`persistence_failed`。
- `04 Handoff Notes` 已删除旧固定侧边栏画板；最新交接节点为 `95:5`，透明面板交接节点为 `134:168`。
- 所有本轮更新文案均为中文，可编辑节点未使用截图替代。

## 2026-07-17 AI 长对话重构

- 已新增默认、长对话、生成中、上翻有新回复和 `360px` 紧凑五个最新画板。
- 最新状态节点依次为 `158:168`、`162:205`、`163:248`、`163:410`、`164:328`，按横向顺序排列且互不重叠。
- 宽屏对话采用约 `70% / 30%`，右侧任务上下文可收起；紧凑模式不保留右栏。
- 消息区显示独立滚动条，输入区固定在底部，生成中仍可见但禁止重复提交。
- `02 AI Components` 新增固定输入区、任务上下文栏、跳到最新、生成气泡和紧凑上下文组件，节点记录已写入 `nodes.json`。
- `03 Data Contract` 已补充 `message / proposal` 分类和 `32,000` 字符上下文预算。
- `04 Handoff Notes` 已补充自动跟随、用户上翻、中文输入法和 `56–136px` 输入高度规则。
- 五张最新画板均已截图检查，中文无溢出，按钮和滚动条无重叠。
- 被替代的 `94:11`、`94:20` 已隐藏并标记待删除。Figma 安全策略阻止未经确认的永久删除，暂不作为当前版本节点。
