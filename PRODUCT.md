# 产品说明

Hermes 待办桌宠是一个轻量、常驻的 Windows 个人待办入口，不定位为完整项目管理系统。

## 已完成的本地第一版

- 闲置时显示桌宠式悬浮入口，点击后展开右侧面板。
- 顶部提供 `今日待办` 和 `周期任务`。
- 今日待办由今日手动待办、今日周期条目和所有未完成逾期条目组成。
- 逾期任务保留原日期；排序为逾期、今日周期、今日手动、已完成。
- 周期计划直接保存确定日期的条目，不运行重复规则引擎。
- 手动计划支持标题、主题、说明和多个日期条目；详情使用 Markdown。
- 健身、学习等领域共用 `CyclePlan + CyclePlanEntry + contentBlocks`，未知内容块也能通用展示。
- 本地 JSON 持久化、最近 7 份备份、损坏恢复、导入和导出。
- 单实例、正式包开机启动、中文托盘和关闭隐藏。

## 当前产品边界

- 只服务一名用户和一台 Windows 电脑。
- 只处理日期，不处理具体时间和到点提醒。
- 不连接 Hermes、飞书或模型服务。
- AI 安排按钮只保留禁用占位，不会生成或修改计划。
- 浏览器 5178 页面用于前端预览；正式使用应安装或运行 Electron 桌面版。

## 后续方向

1. 由 Hermes 提供计划草稿、长期任务和同步接口。
2. 桌面端展示 Agent 建议，所有写操作先由用户确认。
3. 飞书消息和多维表格由 Hermes 代理，桌面端不保存飞书密钥。
4. 在数据和同步稳定后，再评估系统通知、具体时间和桌宠拖拽位置记忆。
