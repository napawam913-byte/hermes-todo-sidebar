# 产品说明

Hermes 待办桌宠侧边栏是一个个人使用的 Windows 桌面提醒工具。它不是完整项目管理系统，而是一个轻量、常驻、像桌宠一样待在桌面上的待办入口。

## 目标用户

长期在电脑前工作，需要随时记录和查看待办事项的人。

## 第一阶段目标

- 闲置时只显示桌宠式悬浮入口。
- 点击桌宠后展开右侧待办面板。
- 面板顶部提供 `今日待办` 和 `周期计划` 两个入口。
- 今日待办按日期读取周期计划条目，并合并手动待办。
- 周期计划使用统一 JSON/Markdown 内容块展示健身、学习等不同主题。
- 第一版不展示具体时间，只按 `YYYY-MM-DD` 匹配今天。
- 支持新增手动待办、完成待办、筛选、来源标签和同步状态占位。
- 使用本地持久化保存手动待办和周期计划完成状态，避免刷新或关闭前端预览后丢失。

## 第二阶段目标

- 通过 Hermes 云端同步待办事件。
- 由 Hermes 负责转发飞书机器人消息。
- 由 Hermes 记录飞书多维表格或其他日志目标。
- 接入 Hermes/AI 生成周期计划草稿，用户确认后再写入周期计划。
- 再讨论是否启用提醒时间、稍后、重复规则和 Windows 系统通知。

## 第三阶段目标

- 桌宠可拖拽、记忆位置、贴边吸附。
- 托盘菜单、开机自启动、快捷键唤起。
- 支持更丰富的计划策略，例如饮食记录、训练重量追踪、学习章节进度和任务分组。
